// styles
import styles from "./SkillsSection.module.scss";

const SkillsSection = (): JSX.Element => {
  return <section className={styles["skills"]}>SkillsSection</section>;
};

export default SkillsSection;
