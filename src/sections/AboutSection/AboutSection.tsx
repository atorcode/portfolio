// styles
import styles from "./AboutSection.module.scss";

const AboutSection = (): JSX.Element => {
  return <section className={styles["about"]}>AboutSection</section>;
};

export default AboutSection;
